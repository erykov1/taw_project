import { NoteCategoryPipe } from './note-category.pipe';

describe('NoteCategoryPipe', () => {
  it('create an instance', () => {
    const pipe = new NoteCategoryPipe();
    expect(pipe).toBeTruthy();
  });
});
